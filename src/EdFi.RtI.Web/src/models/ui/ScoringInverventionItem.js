import { DynamicSlot } from "./DynamicSlot"

/**
 * This class represents the structure of the row items of
 * the InterventionScoringListView
 */
export class ScoringInterventionItem {
    intervention = {
    }; // Intervention DTO

    slot = new DynamicSlot();
}
