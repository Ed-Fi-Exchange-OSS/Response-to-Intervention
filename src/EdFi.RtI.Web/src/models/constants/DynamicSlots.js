export const DynamicSlotStatus = {
  committed: "committed",
  dirty: "dirty",
  rollbacked: "rollbacked"
}

/**
 * The keys for this object should correspond to the DynamicSlotStatus values
 */
export const DynamicSlotColors = {
  committed: "green",
  dirty: "orange",
  rollbacked: "red"
}
